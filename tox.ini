[flake8]
max-line-length = 79
max-complexity = 10

[tox]
envlist = py38

[testenv]
# install pytest in the virtualenv where commands will be executed
deps =
        pytest
        -rrequirements.txt
commands = pytest